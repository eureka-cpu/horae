[package]
name = "cssgen"
version = "0.1.0"
edition = "2024"
publish = false

# Dev-only tool: generates the utility stylesheet from the design scale.
# Std-only, no dependencies, so it builds offline in the Nix check sandbox.

[[bin]]
name = "cssgen"
path = "src/main.rs"

[workspace]
members = ["crates/horae", "crates/core"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = 0
strip = "debuginfo" # strip residual DWARF — prevents binaryen/wasm-opt crash
